# Agent notes

This is the **onboarding visualizer** kit: empty until `prospect/` is filled.

1. Read `.cursor/skills/onboarding-visualizer/SKILL.md`.
2. Load **Bluestone Fluency** (`model.md` and the rest of that skill). Resolve it from the installed `bluestone-fluency` skill, `BLUESTONE_FLUENCY_PATH`, or a local clone of https://github.com/leafleaf90/bluestone-pim-ai-devkit-public (`skills/bluestone-fluency/`). If you cannot find it, stop and send the user that GitHub URL (`npx skills add leafleaf90/bluestone-pim-ai-devkit-public@bluestone-fluency`). Do not use a hardcoded machine path.
3. Inventory `prospect/` only. Propose SQLite + PIM tree. Wait for confirmation. Then convert, ask for `.env` keys, browse, sync via `server/utils/mapi`.

Do not copy another engagement’s mappings into this repo.
