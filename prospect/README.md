Drop prospect source files here (XML, Excel, CSV, JSON, or a nested dump).

This folder is gitignored except this README. Do not commit catalog extracts.

Then tell the agent:

Look in prospect/ for source files. Analyze them and set up the local catalog.
